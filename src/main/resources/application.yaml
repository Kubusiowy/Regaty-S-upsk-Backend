ktor:
  application:
    modules:
      - com.example.ApplicationKt.module
  deployment:
    port: ${APP_PORT}
jwt:
  domain: ${JWT_DOMAIN}
  audience: ${JWT_AUDIENCE}
  realm: ${JWT_REALM}
  secret: ${JWT_SECRET}
